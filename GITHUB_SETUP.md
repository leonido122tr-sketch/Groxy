# Инструкция по созданию репозитория на GitHub

## Шаг 1: Настройка Git (если еще не настроено)

Выполните в терминале:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

## Шаг 2: Создание первого коммита

После настройки git выполните:

```bash
git commit -m "Initial commit: Groxy app v1.02"
```

## Шаг 3: Создание репозитория на GitHub

1. Перейдите на https://github.com и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `groxy` (или другое имя)
   - **Description**: `Groxy - строительный калькулятор для Android`
   - **Visibility**: 
     - ✅ **Private** - если хотите скрыть код
     - ⭕ **Public** - если хотите открытый доступ
   - ❌ **НЕ создавайте** README, .gitignore или license (у нас уже есть)
4. Нажмите **"Create repository"**

## Шаг 4: Подключение локального репозитория к GitHub

После создания репозитория на GitHub выполните команды (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
git remote add origin https://github.com/YOUR_USERNAME/groxy.git
git branch -M main
git push -u origin main
```

Если GitHub запросит авторизацию:
- Используйте **Personal Access Token** (не пароль)
- Создайте токен: Settings → Developer settings → Personal access tokens → Tokens (classic)
- Или используйте GitHub CLI: `gh auth login`

## Готово! 🎉

Ваш код теперь на GitHub. В дальнейшем для обновления используйте:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## Важно!

Следующие файлы **НЕ** попадут в репозиторий (защищены .gitignore):
- ✅ Keystore файлы (`*.keystore`, `keystore.properties`)
- ✅ Собранные APK/AAB файлы
- ✅ `node_modules/`
- ✅ `.env*` файлы с секретами
- ✅ Собранные билды (`build/`, `out/`, `.next/`)

Это правильно - эти файлы не должны быть в репозитории!

