# Инструкция по отправке кода на GitHub

## ✅ Уже выполнено:
- Git настроен
- Первый коммит создан
- Remote подключен: `https://github.com/leonido122tr-sketch/groxy.git`
- Ветка переименована в `main`

## 📋 Что нужно сделать:

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на: **https://github.com/new**
2. Заполните форму:
   - **Repository name**: `groxy`
   - **Description**: `Groxy - строительный калькулятор для Android`
   - **Visibility**: Private или Public (на ваш выбор)
   - ❌ **НЕ создавайте** README, .gitignore или license (у нас уже есть)
3. Нажмите **"Create repository"**

### Шаг 2: Создайте Personal Access Token

1. Перейдите на: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: `Groxy Project`
   - **Expiration**: `No expiration` (или выберите срок)
   - **Select scopes**: Отметьте ✅ **`repo`** (все права репозитория)
4. Нажмите **"Generate token"**
5. **ВАЖНО**: Скопируйте токен сразу (он показывается только один раз!)

### Шаг 3: Отправьте код на GitHub

Выполните команду:
```bash
git push -u origin main
```

Когда Git запросит авторизацию:
- **Username**: `leonido122tr-sketch`
- **Password**: Вставьте ваш Personal Access Token (НЕ пароль от GitHub!)

## 🔐 Альтернативный способ (с токеном в URL)

Если не хотите вводить токен каждый раз, можно добавить его в URL:

```bash
git remote set-url origin https://YOUR_TOKEN@github.com/leonido122tr-sketch/groxy.git
```

(Замените `YOUR_TOKEN` на ваш токен)

Затем просто:
```bash
git push -u origin main
```

## ✅ После успешной отправки

Ваш код будет доступен по адресу:
**https://github.com/leonido122tr-sketch/groxy**

## 📝 В дальнейшем для обновления

```bash
git add .
git commit -m "Описание изменений"
git push
```

